services:
  web:
    build: .
    command: >-
      sh -c 'bin/rails db:prepare && bin/rails server -b 0.0.0.0'
    environment:
      RAILS_ENV: development
    ports:
      - "3000:3000"
    volumes:
      - sqlite_data:/app/storage

volumes:
  sqlite_data:
